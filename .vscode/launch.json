{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Dev (Electron)",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run dev",
      "cwd": "${workspaceFolder}"
    },
    {
      "name": "Dev (Web only)",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run dev:web",
      "cwd": "${workspaceFolder}"
    }
  ]
}
